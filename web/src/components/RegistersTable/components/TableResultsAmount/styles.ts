import styled from "styled-components";

export const TableResultsAmountContainer = styled.div`

  width: auto;
  height: auto;

  span {
    font-weight: 400;
    font-size: 1rem;
    color: ${props => props.theme["slate-800"]};
  }

`